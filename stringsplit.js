text = "Please visit Microsoft!" + "AND ME";


comma = text.split(",")    // Split on commas
space = text.split(" ")    // Split on spaces
pipe =text.split("|")    // Split on pipe
console.log(comma);
console.log(space);
console.log(pipe);
